#include <bits/stdc++.h>

using namespace std;

int main (void ) {
    int t;
    scanf("%d", &t);

    while(t--) {
        int n;
        scanf("%d", &n);

        if (n % 2) printf("1\n");
        else (printf("0\n"));
    }

    return 0;
}